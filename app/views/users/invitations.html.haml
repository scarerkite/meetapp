.row
  .col-md-6
    %h3 
      Friend requests
    - current_user.requested_friendships.each do |u|
      %h3
        = link_to u.name, "/users/#{u.id}"
      / = link_to "Accept", accept_friend(u), method: :post
      / = link_to "Decline", decline_friend(u), method: :post


  .col-md-6
    %h3
      Event invites
    - @invited_events.order('created_at ASC').each do |event|
      %h3 
        = link_to event.title, event
      %p
        = event.description
      %p
        = event.date.strftime("%d/%m/%y")
      / = link_to "Accept", accept_invitation(event), method: :post
      / = link_to "Decline", decline_invitation(event), method: :post